# 2026-08-24

## 任务：分析 medkit 项目架构并产出 agent.md
- 通读了 README/CLAUDE/spec/package.json/vite.config/vercel.json/middleware.ts + 前端核心（store/types/App/voice/*/agents/*/EncounterScreen）+ 后端（server.py/voice_agent.py/requirements）+ guidelines/data 层 + agent/skills。
- 产出 `agent.md`（项目根）：① 分层架构图；② 前端架构（Store 单例+useSyncExternalStore、3D/R3F、LiveKit 客户端、Managed Agent 客户端）；③ 后端架构（FastAPI 双进程：server.py 代理 + voice_agent.py Worker）；④ 两条核心数据流；⑤ 智能体改写约定（13 条 + 速查表）；⑥ 模型路由；⑦ 验证测试；⑧ 边界与"不要做"清单。
- 重点约束已记录：密钥只在后端、前后端 customTools schema 必须同步、不引 tsx/状态库、儿科父母代述哈希两端一致、POLYCLINIC_BED_INDEX=-10 哨兵。
- 用户用词 "agnet.md" 实为 agent.md；已建为 agent.md。
